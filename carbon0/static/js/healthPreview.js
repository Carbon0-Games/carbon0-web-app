// show when this plant was last updated, and what the status was
export function footprintPlot(elemID, lastStatus, lastUpdated) {
    // get the HTML element we'll be making changes to
    let ctx = document.getElementById(elemID);
    // TODO: show the last time the plant was updated on the DOM
    // TODO: do the same for the latest health check result
}
